# Neon pooled connection string — see "Database setup" in CLAUDE.md
DATABASE_URL=

# better-auth reads this automatically to sign sessions/cookies
BETTER_AUTH_SECRET=

# Used to build invite links and as the auth baseURL — falls back to
# http://localhost:3000 locally if unset, but must be set in production
NEXT_PUBLIC_APP_URL=
